<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.znty.rrs.mapper.ForbiddenPoolQueryMapper">
    <!-- 风险池查询 mapper，对应表：ip_pool_status（证券池状态），覆盖禁投池、观察池和黑名单质押库 -->

    <!--
      分页查询禁投池证券列表。
      wind_cbondissuer 为债券+主体粒度，JOIN 前先按 s_info_compcode 去重，避免同主体多债行膨胀。
    -->
    <select id="queryForbiddenPoolPage" resultType="com.znty.rrs.entity.forbiddenpoolquery.ForbiddenPoolQueryDto">
        SELECT ips.id
               ,ips.security_code
               ,ips.security_short_name
               ,ips.adjuster_name
               ,ips.entry_time
               ,ips.target_pool_id
               ,ips.adjust_batch_no
               ,p.pool_name AS target_pool_name
               ,ips.security_type
               ,dst.security_type_name
               ,dst.category_type
               ,COALESCE(wci.s_info_compname, bi.issuer, ips.security_short_name) AS issuer
               ,bi.maturity_date
               ,bi.delist_date
               ,bi.repurchase_date
        FROM ip_pool_status ips
        INNER JOIN ip_investment_pool p ON p.id = ips.target_pool_id
                                       AND p.is_deleted = 0
                                       AND p.pool_type IN ('forbidden', 'observe', 'blacklist')
        LEFT JOIN rrs_securityinfo bi ON bi.wind_code = ips.security_code
        LEFT JOIN dict_security_type dst ON dst.security_type = ips.security_type AND dst.is_deleted = 0
        LEFT JOIN (
            SELECT s_info_compcode
                   ,MAX(s_info_compname) AS s_info_compname
            FROM ais_inv_ods.wind_cbondissuer
            WHERE used = 1
            GROUP BY s_info_compcode
        ) wci ON wci.s_info_compcode = ips.security_code
             AND ips.security_type = 'company'
        <where>
            ips.is_deleted = 0
            AND ips.audit_status = '20'
            <if test="viewablePoolIds != null and viewablePoolIds.size() == 0">
                AND 1 = 0
            </if>
            <if test="viewablePoolIds != null and viewablePoolIds.size() > 0">
                AND ips.target_pool_id IN
                <foreach collection="viewablePoolIds" item="poolId" open="(" separator="," close=")">
                    #{poolId}
                </foreach>
            </if>
            <if test="securityCode != null and securityCode != ''">
                AND ips.security_code LIKE CONCAT('%', #{securityCode}, '%')
            </if>
            <if test="securityShortName != null and securityShortName != ''">
                AND ips.security_short_name LIKE CONCAT('%', #{securityShortName}, '%')
            </if>
            <if test="securityType != null and securityType != ''">
                AND ips.security_type = #{securityType}
            </if>
            <if test="securityStatus == 'active'">
                AND dst.category_type = 'bond'
                AND bi.maturity_date &gt;= CURDATE()
            </if>
            <if test="securityStatus == 'matured'">
                AND dst.category_type = 'bond'
                AND bi.maturity_date &lt; CURDATE()
            </if>
            <if test="entryTimeStart != null and entryTimeStart != ''">
                AND ips.entry_time &gt;= #{entryTimeStart}
            </if>
            <if test="entryTimeEnd != null and entryTimeEnd != ''">
                AND ips.entry_time &lt;= CONCAT(#{entryTimeEnd}, ' 23:59:59')
            </if>
            <if test="adjusterName != null and adjusterName != ''">
                AND ips.adjuster_name LIKE CONCAT('%', #{adjusterName}, '%')
            </if>
            <if test="issuer != null and issuer != ''">
                AND COALESCE(wci.s_info_compname, bi.issuer, ips.security_short_name) LIKE CONCAT('%', #{issuer}, '%')
            </if>
        </where>
        ORDER BY ips.entry_time DESC
                 ,ips.id DESC
    </select>

    <!-- 查询风险池中出现的证券类型选项 -->
    <select id="querySecurityTypeList" resultType="com.znty.rrs.entity.common.SecurityTypeOptionDto">
        SELECT DISTINCT ips.security_type
                       ,dst.security_type_name
        FROM ip_pool_status ips
        INNER JOIN ip_investment_pool p ON p.id = ips.target_pool_id
                                       AND p.is_deleted = 0
                                       AND p.pool_type IN ('forbidden', 'observe', 'blacklist')
        LEFT JOIN dict_security_type dst ON dst.security_type = ips.security_type AND dst.is_deleted = 0
        WHERE ips.is_deleted = 0
          AND ips.audit_status = '20'
          <if test="viewablePoolIds != null and viewablePoolIds.size() == 0">
              AND 1 = 0
          </if>
          <if test="viewablePoolIds != null and viewablePoolIds.size() > 0">
              AND ips.target_pool_id IN
              <foreach collection="viewablePoolIds" item="poolId" open="(" separator="," close=")">
                  #{poolId}
              </foreach>
          </if>
        ORDER BY dst.sort_order ASC
                 ,ips.security_type ASC
    </select>

</mapper>
